# Dev: leave empty to use Vite proxy (same-origin relative URLs)
# Prod build served from FastAPI: also leave empty (same origin on :8000)
# Vite dev with API on another host: set full URL
# VITE_API_BASE_URL=http://127.0.0.1:8000
