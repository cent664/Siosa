{
  "stages": [
    {
      "id": "0.1",
      "title": "Corpus",
      "chosen": "18-page allowlist",
      "summary": "Offline poewiki ingest into a local index.",
      "detail": "<p><strong>Where data comes from:</strong> Path of Exile 1 articles on <a href=\"https://www.poewiki.net\">poewiki.net</a>, listed in <code>seed_pages.yaml</code> (18 mechanic pages) for offline ingest.</p><p><strong>How we decided:</strong> A small allowlist keeps ingest fast and answers focused on core mechanics — not the full ~16k-article wiki.</p><p><strong>What runs:</strong> <code>poe-ingest</code> writes <code>data/raw/</code>, <code>chunks.jsonl</code>, and a Chroma vector index. With <code>RETRIEVAL_MODE=live</code> (default), Ask can skip the local corpus and fetch pages at query time.</p>",
      "alternatives": [
        {
          "label": "Full wiki dump",
          "opaque": true,
          "detail": "<p><strong>Industry pattern:</strong> Mirror the entire wiki (dump or crawl-all) into one search index.</p><p><strong>Here:</strong> Not implemented — would raise storage, ingest time, and noise. See <em>Scaling to the full wiki</em> in this doc.</p>"
        },
        {
          "label": "Live fetch per query",
          "opaque": false,
          "detail": "<p><strong>Industry pattern:</strong> Fetch missing pages at question time (dynamic RAG).</p><p><strong>Here:</strong> Default via <code>RETRIEVAL_MODE=live</code> — MediaWiki search, fetch, disk cache (<code>data/live_cache/</code>), in-memory chunk + rerank. Set <code>RETRIEVAL_MODE=local</code> to use only the pre-built index.</p>"
        }
      ]
    },
    {
      "id": "0.2",
      "title": "Ask",
      "chosen": "React + FastAPI",
      "summary": "Your question enters the harness API.",
      "detail": "<p><strong>What happens:</strong> The React UI sends your question (and provider choice) to <code>POST /query</code>. No retrieval yet — this step only captures input and settings.</p><p><strong>Also:</strong> <code>POST /settings/provider</code>, optional <code>POST /transcribe</code> for voice.</p>",
      "alternatives": [
        {
          "label": "Streamlit UI",
          "opaque": true,
          "detail": "<p>Earlier prototype UI on port 8501 — removed in favor of React + Vite served from <code>web/dist/</code>.</p>"
        }
      ]
    },
    {
      "id": "0.3",
      "title": "Retrieve",
      "chosen": "Mode-aware retrieve",
      "summary": "Live poewiki (default) or local hybrid + rerank.",
      "detail": "<p><strong>Default (<code>live</code>):</strong> Multi-query fusion on every <code>wiki_search</code> — several poewiki search strings, fetch up to <code>LIVE_WIKI_MAX_PAGES</code> pages, chunk, cross-encoder rerank (~top 5). Not BM25 on the full wiki.</p><p><strong>Local hybrid:</strong> Dense Chroma + BM25 + RRF on the 18-page ingested index only (<code>local</code> or weak-hit fallback in <code>hybrid</code>).</p><p><strong>Note:</strong> <code>chroma_ready=false</code> on production is normal when live-only — no local ingest on the server.</p>",
      "alternatives": [
        {
          "label": "Dense only",
          "opaque": true,
          "detail": "<p><strong>What it is:</strong> Semantic vector search only — good for paraphrases, weaker on exact item names.</p><p><strong>Here:</strong> We always combine dense with BM25.</p>"
        },
        {
          "label": "Sparse only",
          "opaque": true,
          "detail": "<p><strong>What it is:</strong> BM25 / keyword search — strong on exact terms, weaker on rephrased questions.</p><p><strong>Here:</strong> Used together with dense in hybrid mode.</p>"
        },
        {
          "label": "No rerank",
          "opaque": true,
          "detail": "<p><strong>What it is:</strong> Take top fusion results without a cross-encoder second pass.</p><p><strong>Here:</strong> Cross-encoder rerank runs when the model loads successfully.</p>"
        }
      ]
    },
    {
      "id": "0.4",
      "title": "Plan",
      "chosen": "LangGraph (optional)",
      "summary": "Planner-driven multi-search when needed.",
      "detail": "<p><strong>LangGraph</strong> runs for typical Claude/GPT-4 Ask paths: a planner emits <strong>1–4</strong> retrieve subtasks (verbatim question plus short wiki keywords). Compare questions are a common case, not the only one.</p><p>Each subtask still uses <strong>multi-query fusion</strong> inside <code>wiki_search</code>.</p><p><strong>Linear RAG</strong> (<code>linear_rag</code> trace) when provider is <code>stub</code> or <code>linear</code> — one retrieval pass, fusion still applies in live mode.</p>",
      "alternatives": [
        {
          "label": "Linear only",
          "opaque": true,
          "detail": "<p><strong>What it is:</strong> Single retrieval pass with the user question — the classic RAG loop.</p><p><strong>Here:</strong> Always available; used when LangGraph does not plan extra steps.</p>"
        },
        {
          "label": "Rule-based planner",
          "opaque": true,
          "detail": "<p><strong>What it is:</strong> Heuristics instead of an LLM to split compare questions.</p><p><strong>Here:</strong> Fallback when LLM planning fails.</p>"
        }
      ]
    },
    {
      "id": "0.5",
      "title": "Generate",
      "chosen": "UI provider",
      "summary": "LLM or stub writes from retrieved chunks only.",
      "detail": "<p><strong>What we do:</strong> The selected provider (stub, Claude, or GPT-4; Ollama locally when <code>POE_ENABLE_OLLAMA=true</code>) generates an answer using <strong>only</strong> the retrieved excerpts — not the whole corpus.</p><p><strong>Stub:</strong> Returns a wiki excerpt with no LLM call.</p><p><strong>LLM modes:</strong> Short grounded answer with citations; system prompt enforces PoE 1 and excerpt-only rules.</p>",
      "alternatives": [
        {
          "label": "Stub",
          "opaque": true,
          "detail": "<p>No generation model — fastest path, shows top chunk text for debugging retrieval.</p>"
        },
        {
          "label": "Bedrock",
          "opaque": true,
          "detail": "<p><strong>AWS Bedrock</strong> as a cloud provider — supported via <code>.env</code> for generation or judges, not exposed in the UI toggle.</p>"
        }
      ]
    },
    {
      "id": "0.6",
      "title": "Judge",
      "chosen": "Retrieval + generation judges",
      "summary": "Five inline judges when enabled; gold set on /evaluate.",
      "detail": "<p><strong>Inline Ask (<code>INLINE_EVAL=true</code>):</strong> Five LLM judge calls — context precision, context recall, faithfulness, relevance, prompt adherence (custom prompts; RAGAS-inspired names, not the RAGAS library).</p><p><strong>Gold set:</strong> 10 labeled questions in <code>gold.jsonl</code>; <code>POST /evaluate</code> gives exact page-title retrieval P/R plus optional extraction overlap.</p><p><strong>Production:</strong> <code>INLINE_EVAL=false</code> skips all judges for speed.</p>",
      "alternatives": [
        {
          "label": "Skip inline judges",
          "opaque": true,
          "detail": "<p>Set <code>INLINE_EVAL=false</code> in <code>.env</code> to skip generation judges on <code>/query</code> (faster, no 1–5 scores in UI).</p>"
        },
        {
          "label": "Gold /evaluate",
          "opaque": true,
          "detail": "<p><strong>Harness Evaluate button</strong> runs retrieval precision/recall plus generation judges and optional extraction vs a gold answer — for regression testing, not casual Q&A.</p>"
        }
      ]
    },
    {
      "id": "0.7",
      "title": "Respond",
      "chosen": "Answer + trace",
      "summary": "Answer + citations; trace when dev UI.",
      "detail": "<p><strong>Always:</strong> Grounded answer and wiki citations (title, URL, snippet).</p><p><strong>Local dev:</strong> <code>quality_scores</code> (five metrics when inline eval on), full <code>trace</code> — plan, tool calls, timing, LLM I/O.</p><p><strong>Production booth:</strong> Answer + Sources only — no scores, trace, or timing in the UI.</p>",
      "alternatives": []
    }
  ]
}
